{% extends '::baseAdmin.html.twig' %}

{% block content %}
    <h1>Правка Слайдера</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('slider') }}">
            Обратно к списку
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
